---
name: article-distiller
description: 面向希望了解 AI、进入 AI 领域但未必具备技术背景的读者，抓取文章并基于可审计证据生成通俗、有趣、有独特见解的中文深度解读母稿；适用于论文、技术报告、产品分析和 AI 资讯。
---

# 深度文章解读

把任意 AI 文章解读成看得懂的可视化文章，而不是扩写摘要。成品应回答一个中心问题，并把判断、证据、实验、案例、限制和行动含义组织成连续论证。

写作总纲是：**真实为底，讲透为骨，人话为形，朋友感为声**。可信不等于报告腔，专业不等于术语密度高；文章既要经得起核对，也要让非专业读者愿意顺着读下去。

成稿再用“价值、可读性、共鸣”三项检查：先让目标读者得到明确的信息或解释，再用结构和人话降低理解成本，最后用材料中真实存在的处境、冲突和结果建立共鸣。不能只顾严谨而不讲清事件，也不能只有情绪和形式却没有信息增量。

主编审校按四层检查文章本身：**灵魂**是面向目标读者的中心主旨，**骨架**是有主次、有承接的逻辑推进，**血肉**是扎实素材与准确、生动的人话表达，**颜值**是有点击动机的标题、舒适排版和规范细节。四层服务于内容，不能用“颜值”掩盖前三层缺口。

## 目标读者与母稿用途

- 默认读者对 AI 新闻、工具、产品和行业机会感兴趣，希望建立知识框架或进入 AI 领域，但不假设其具备算法、工程或研究背景。把读者视为聪明的非专业人士，不写成儿童科普，也不让术语成为门槛。
- 首次出现且会阻碍理解的 AI 专有名词、缩写、模型指标和技术机制，先用一句人话说明“它是什么、解决什么问题”，确有必要时再补准确术语和条件。专有名词始终保留官方或原始拼写，不用编辑自创的中文说法替换；主标题、章节标题、图表标题和表头不追加翻译括注。存在稳定常用中文名且不说明会妨碍理解时，导语或正文首次出现必须写成 `原名（常用中文名）`，不能只依赖名词解释脚标代替正文括注；没有稳定译名或中文说法会造成语义偏移时，直接保留原名，把作用写进正文或名词解释。同名后文不重复标注。人名、品牌名、产品名、代码、模型编号和文件名不机械翻译；`AI`、`GPU`、`CPU`、`App`、`PDF`、`URL`、`Wi-Fi`、`ChatGPT` 等已进入大众语境的名称或缩写直接使用，也不生造音译。
- 每篇文章应让读者读完能用自己的话复述三件事：发生了什么或机制是什么，为什么与 AI 领域相关，以及作者基于材料得出的独特判断。
- 成品是后续博客、视频和其他内容形态的高质量母稿：需要清楚的一句话主旨、连续故事或机制主线、可独立提取的章节观点、可视化关系和有记忆点的结论。母稿仍须像自然文章，不能写成平台化口播提纲或碎片组件合集。
- 独特见解必须来自事实之间的机制、矛盾、取舍或后果，不写“AI 是双刃剑”“未来值得关注”这类换一篇文章也成立的空洞观点。

## 执行

运行时要求 Python 3.10+。依赖固定在 `requirements.txt`，显式包含网页正文提取使用的 `trafilatura`、新版 `lxml` 所需的 `lxml-html-clean`、动态媒体发现使用的 `playwright`、LLM 客户端 `openai`、Word 读取使用的 `python-docx` 和 PDF 读取使用的 `pypdf`。CLI 启动时会检查网页抓取与动态媒体依赖，处理 PDF/DOCX 时按需检查文件依赖；缺失时优先使用当前 Python 自动安装，Playwright 会先尝试系统 Chrome/Edge/Chromium，没有可用浏览器时自动安装 Chromium。网络或权限不允许时必须打印可执行的处理命令，不能静默跳过媒体检查。始终使用安装者自己的 Python 3.10+ 环境，不硬编码个人解释器路径。

入口是 `scripts/run.py`，固定输出深度文章 `full`：

```bash
PY=python3
$PY <skill-root>/scripts/run.py <URL> --both -o <output-base>
```

安装后可先运行环境预检：

```bash
$PY <skill-root>/scripts/run.py --check
$PY <skill-root>/scripts/run.py --check --no-llm
```

预检会检查 Python 版本、网页抓取依赖、PDF/Word 所需模块、LLM 配置以及按需使用的系统工具。缺少 Python 模块时会用当前解释器自动安装；系统组件、API Key 或 OCR 工具缺失时会给出明确的处理命令或替代方案。正常运行也会自动执行同一预检。

第一个参数也可以直接是本地 `.txt`、`.md`、`.html`、`.pdf`、`.docx` 或 `.doc` 文件；PDF 使用 `pypdf` 提取文字，DOCX 使用 `python-docx` 读取段落和表格，旧 `.doc` 由 LibreOffice `soffice` 转换后读取。扫描型 PDF 没有文本层时会明确提示先 OCR；`.doc` 环境缺少 LibreOffice 时会提示安装或另存为 `.docx`。`--from-text` 是同一能力的显式写法，可配合 `--title`、`--author` 覆盖文件元数据。

完整文章始终保留“研究账本 → 正文写作 → 主编审校”三阶段。运行默认使用流式模型响应，把原文与成功抓取附件保存为来源快照，并把三个模型阶段分别保存到 `<output-base>.distill-cache`；网络超时、代理 524 或同一 URL 暂时被反爬拦住后用原命令重跑，只续跑指纹一致且尚未完成的阶段。网页正常抓取时始终以最新正文计算指纹，只有抓取失败时才恢复 24 小时内的真实材料快照。写作阶段保留原始文章和完整研究账本，但不重复传输研究阶段已经消化的附件全文。单一输出模式的主编仍完整读取研究账本与草稿，但默认只回传实际修改字段，由本地确定性合并；补丁无效时自动切回完整修订协议，合并后仍执行版本择优和严格发布门禁。不得为了缩短时间默认切换低质量模型、跳过研究、跳过主编审校或削减高优先级证据。

没有 LLM 配置时，使用 `--source-only` 生成仅面向长文的 prompt 包；已有 pack 和解读 JSON 时使用 `--render`。网页无法抓取时，取得可信正文后使用 `--from-text`，不要基于空正文继续写作。CLI 会自动安装缺少的 Python 依赖；若自动安装失败，按错误信息中的当前解释器命令安装后重试。PDF/Word 转换不需要上传第三方服务，正文在本地提取后才进入 LLM 阶段。

默认从原文直链中最多抓取 5 个高价值官方附件（版本公告、模型卡、文档、仓库、论文或许可证）。同站且标题、路径与当前主题高度重合的延伸文章优先于组织首页、标签页、作者页和通用导航页，不能让低信息页面挤占附件名额。明确独立于发布方的材料必须用 `--independent-url`；普通 `--evidence-url` 只登记为官方或补充材料，不能产生“交叉验证”。无论 GitHub 仓库是用户主链接还是自动发现的官方附件，都使用受限目录遍历读取最多 6 个关键文件，不请求全量递归树；除文档和 Python 外，还要识别 Rust、Go、Java、Scala、Swift、C/C++ 与 JavaScript/TypeScript 等核心实现文件，优先排名、模型、训练、检索、流水线、过滤、权限、审批、策略和配额代码，压低行为准则、变更日志等元文件。原文及已抓取附件中的真实图片、视频和音频可以进入长文，但必须来自抓取注册表并用 `after_section_id` 放回相关论证段；它们不是 AI 生图，也不自动证明发布方的性能结论。

浏览器、搜索工具或站内搜索可用时，不能只依赖原文直链。读取 [references/research-and-media.md](references/research-and-media.md)，围绕主体、产品/论文、关键主张和媒体演示扩展检索，默认实际读取 2-5 个高价值补充来源。对标站只用于发现上游线索和可复用的表达模式，不作为事实来源；素材必须追到原始发布者，或明确标注为第三方加工版。CLI 对远程网页默认启动 Playwright，读取 JavaScript 渲染后的图片、视频、音频、iframe、CSS 背景图、懒加载节点和媒体网络请求，并自动合并进注册表。若站点对无会话浏览器返回 403，而 Codex Browser 可用，必须由代理直接检查用户可访问的页面、自动生成带 `media_discovery.status=completed` 的 `page-assets.json` 并传给 `--page-assets`，不把这一步留给用户手工提醒。只有用户显式使用 `--no-dynamic-media` 才允许跳过，并明确说明成品不具备动态媒体完整性保证。

采用外语媒体时必须完成中文化再表达：自然中文图注、原始语言标记，以及具体的中文观看重点或读图提示。能可靠还原的英文图表优先转成合适的中文 HTML 交互组件，原图保留在证据图库；不能可靠还原时保留原图并给中文提示。不得声称制作了并不存在的字幕，也不得猜测画面、数据或逐字翻译。素材选择以信息增量为准，气氛视频、品牌片头、重复长图和装饰图仍须省略。

需要追踪图表内来源时显式开启 `--chart-ocr`。它只处理最多 5 张疑似图表、单图最多 12 MB，优先使用 macOS Vision，缺失时回退 Tesseract。OCR 文本只作为查找上游来源的线索，不是逐字引文或独立证据；默认关闭以避免不必要的下载与延迟。

用户明确要求在原始素材之外补充解释配图时，读取 [references/article-imagegen.md](references/article-imagegen.md)。解释图只用于空间、材质、尺度、氛围或难以代码化的机制与案例环境，必须标注为“AI 概念示意”，不能替代跑分、截图、真实人物或案例证据。流程、层级、时间、对比和因果关系优先使用 HTML/CSS 组件；已有来源媒体或代码化组件能回答同一问题时不再补重复位图。CLI 默认仍不调用图片接口，避免他人安装后无感产生费用；但当前任务一旦已获用户生图授权，且成稿存在非空 `illustration_plan`，最终交付必须显式使用 `--article-images generate`，不能只留下策划 JSON。AI 位图至少比较两个候选并完成视觉审核后才能发布；文字修订后优先使用 `reuse`，relay 不可用时明确报告缺少的配置，不静默交付缺图版本。

涉及参数和配置时读取 [references/cli.md](references/cli.md)。开始写深度文章或检查成品时读取 [references/article-depth.md](references/article-depth.md)、[references/human-writing.md](references/human-writing.md)、[references/editorial-patterns.md](references/editorial-patterns.md)、[references/research-and-media.md](references/research-and-media.md) 和 [references/chinese-grammar-review.md](references/chinese-grammar-review.md)；对标模式不得作为事实来源，中文语病审校坚持明确错误、语境风险与文风建议分层。修改证据状态时必须读取 [references/evidence-schema.md](references/evidence-schema.md)。修改编辑门禁时读取 [references/editorial-quality.md](references/editorial-quality.md)。

规划或审查图表、播放器、交互卡和多层结构时读取 [references/visual-selection.md](references/visual-selection.md)，先判断内容关系和读者问题，再选组件；视觉多元来自关系不同，不来自随机换皮。

每次处理用户给出的新文章时，同时检查本次实际读取的原始文章、官方附件、论文、代码仓库与原创研究是否构成新的 A/B 级信息源。符合条件且未收录时，读取 [references/source-registry.md](references/source-registry.md)，补充到用户运行时信息源清单；默认位于 `~/.article-distiller/source-registry.md`，可由 `ARTICLE_DISTILLER_SOURCE_REGISTRY` 覆盖。对比站、聚合页、媒体转述、未读取链接和只有线索价值的页面不得入库。

本 Skill 只负责深度文章 HTML/Markdown。若项目另行安装了一页纸和小红书卡片 Skill，再由它们各自完成对应产物；本 Skill 不生成或打包这些格式。

## 不变量

- 先建立原子主张、实验和案例研究账本，再写文章；研究与写作默认分阶段完成。
- `importance=high` 的主张必须进入正文，或在 `editorial_coverage.omitted_claims` 中给出具体理由。
- `covered_claim_ids` 只是编辑声明，不能代替实际覆盖。严格生成还会逐模式检查发布内容中的关键短语、数字、型号和否定边界；`fact_check`、`research_ledger`、`source_notes` 中出现的文字不计为正文覆盖。
- 原文确有实验时，完整保留问题、环境、样本、模型、指标、结果、对照和外推限制。不能只摘最好看的数字。
- 研究账本中的数量、比例、价格、性能、样本和变化幅度标为 `claim_kind=metric`。`importance=high` 的指标主张必须建立 `number_stories`，同时写清主数字、单位、分母或计时口径、时间、适用范围、对照或变化、证据限制和登记来源；口径不完整时只能按普通文字呈现，严格发布不得生成醒目的大数字卡。“未知”“未提供”不算已补齐。`denominator`、`scope`、`period`、`baseline`、`change`、`boundary` 只作为内部审计字段；HTML 使用 `labels` 输出读者能独立看懂的关系名称，如“统计对象、计时口径、对应事件、统计时间、对照情况、结果变化、这个数字不能说明什么”，不能直接显示“分母、范围、边界”等内部术语。
- `number_stories` 首先服务于数字主张审计，不代表每条记录都要公开渲染。同一数字已由正文、`metric_bars`、`rank_bars`、实验详情或来源图完整表达时设为 `suppress_visual=true`，继续参与覆盖与来源检查但不进入 HTML/Markdown。同一章节最多公开一张真正承担论证任务的数字卡，禁止把内部审计记录连续铺成多张大型卡片。
- 数字卡默认使用紧凑样式，只展示主数字、一句自然口径说明和关键限制。只有数字本身就是文章核心结论，而且读者必须同时查看统计对象、时间、对照和变化才能理解时，才允许展开完整数字卡；辅助背景数字即使 `importance=high` 也保持 `display_variant=compact` 或直接 `suppress_visual=true`。不得把“无明确对照、无可计算变化”等内部空值做成公开白框。
- 原文确有案例事件链时才能重建案例。`source_mode=reconstruction` 必须明确表示证据重建，不能伪装成逐字对话；不得编造动作、工具调用或结果。
- 实验、案例和视觉组件用 `after_section_id` 放在其所支撑的论证之后，不能堆到文末。
- 视觉组件先拆分主维度与组内维度，不能一刀切。时间线、流程、因果链、前后变化和数字解释字段沿主阅读顺序展开。`compare_table` 提供三种语义版式：普通顺序说明用 `stacked`；文字较长、需要强调每组语境的平级对象用 `paired`；列名固定、每行字段一致、读者需要逐行逐列快速扫视的二维数据用 `matrix` 表格。例如“环节 × Claude 职责 × 人的职责”优先使用 `matrix`。窄屏不得横向溢出，表格单元格允许自然换行。
- 架构层级、能力分层、上下游或上层依赖下层的关系使用 `layer_stack`，让层级摘要始终可扫读、细节按需展开；流程、时间、平级对比不能为了样式多元伪装成层级。每个视觉先回答一个明确的 `reader_question`，同一章节通常只放一个主视觉，音视频和来源图可作为证据补充。
- 矩阵表超过6行、4列或24个单元格时，只有逐格查数本身是阅读任务才保留；否则按读者问题拆分，或改用 `rank_bars`、`metric_bars`、`layer_stack`、切换卡。不得连续使用三个相同结构的组件，也不得用多个组件重复同一段正文。
- 同一批模型或对象若有两组以上回答不同问题的数值指标，优先用 `metric_bars`，把“读者要判断的问题”做成切换项，每次只展示一个指标。主方案与对照必须保留原始数字和单位，横条只在同一模型、同一指标内归一化，旁边明确倍数及越高/越低是否更好；不同指标不能拼成一个总加速比。固定列、同口径、确实需要逐格查数时才保留矩阵表格。
- 同一数值口径下，多个动作、参数或对象的大小和排序本身是重点时，使用 `rank_bars`。每组保持同一单位和正负方向，按绝对值降序展示；正向与负向参数可以切换，但不同单位、总体、时间和计算口径不能混排。条长只表达组内相对大小，显示值保留原始正负号和单位。
- 普通先后动作继续用 `flow`；只有候选对象经过2至5道真实资格判断、范围逐关收窄时才用 `funnel_flow`。没有真实转化率时，漏斗宽度只表达“越来越少”，不得暗示具体淘汰比例；没有淘汰关系的流程不能为了视觉变化硬改成漏斗。
- 同一指标有明确旧版/新版、调整前/后关系时用 `delta_table`；多个对象需要横看多个固定支持维度时用 `status_matrix`；真实条件会决定结果，并能对应读者行动时用 `decision_table`。普通平级比较、流程和无条件建议清单继续使用原有组件，不为换样式错判关系。
- 互动组件优先用来回答“拖动、切换或点选后，哪个状态、关系或结果发生变化”。复杂的3至7步流程可用 `flow.presentation=stepper` 逐步探索；3至8个且每个都有解释价值的时间节点可用 `timeline.presentation=scrubber` 拖动查看。普通短流程和日期清单保持静态；互动不设数量配额，也不能把关键事实默认藏起来。
- 数据视觉的颜色必须保持固定语义，而不是随机丰富：核心信息和对照方案用蓝色，主方案、正向结果和明确进展用绿色，限制、条件和待确认项用琥珀色，红色只用于材料明确支持的风险、失败或损失。`compare_table.column_roles`、`stat.items[].tone`、`delta_table.rows[].tone`、`status_matrix.cells[].tone` 和 `decision_table.rows[].tone` 使用同一语义表；没有明确语义时留空或使用 `neutral`，不得把任意列强行染成红绿阵营。标签、色块、横条和提示保持一致，三种主题下都要保持可读对比度。
- 只有当“切换条件会改变结果”本身是核心机制时才使用 `interactive_compare`；候选、模式和结果必须来自已核对材料或明确标为机制示意，不能伪装成真实概率或产品界面。
- 原始图表存在可比较对象、可靠基数和清楚计算关系时，可用 `scenario_calculator` 把静态图转成“切换对象 + 调整假设 + 查看结果”的证据情景卡。卡片必须登记原始媒体，区分来源数值、近似图表读数和读者假设；跨平台总体样本不能伪装成单个平台数据。原图保留在页末折叠证据图库供核对，正文无需再次连续照搬同组大图。
- 连续变量存在材料支持的拐点、U形或倒U形关系时使用 `capacity_curve`，用3至5个定性阶段解释方向变化。position 只表示阅读顺序，不生成伪精确结果；必须说明转折点随学生、数据、架构、方法或对应条件变化，不是通用预测器。
- 不同成本、资源或责任项目是否计入会改变判断时使用 `cost_ledger`，让读者切换2至6种真实口径；不需要逐格查数时，不再把同一问题铺成大矩阵表。
- `claim_ids`、实验 id 和证据状态是内部审计字段，发布页不得直接显示 `c5`、`exp-quality` 等编号。
- 原文 URL 不是独立证据。只有实际抓取并登记为 `independent` 的来源才能使主张成为 `cross_checked`。
- 官方模型卡、仓库和文档用于补齐实验与使用细节，但仍属于发布方证据；跨站但独立性不明的材料只能算 `supplemental`。
- 来源媒体只从 `Article.media_assets` 选用；注册表可包含原文及已抓取附件中的真实素材。未登记、重复使用或锚点无效的图片/视频不得发布。登记音频通过 `listening_cards` 组织为真实提示词、原生播放器和具体听感重点；曲目必须绑定 audio `media_id`，并说明官方精选样曲不能代表随机输出、独立盲测或普遍质量。
- 原网页出现视频不构成正文采用理由。视频只有在展示产品操作、实验过程、前后差异、人物关键表述或机制变化，并且正文或静态图无法等价替代时才发布；循环口号、气氛动画、品牌片头和纯装饰首屏视频登记到 `media_omissions`，不占正文篇幅。
- 媒体发现同时检查原始 HTML 中的标准标签、自定义媒体组件和懒加载属性；不能只依赖浏览器最终生成的 `<video>` 节点。原文存在能直接展示中心能力的官方演示视频时，写作阶段至少评估一段，未采用任何演示视频必须在发布审计中提示；多段视频按各自回答的问题分散到相关章节。
- 成稿后的对标文章若出现本稿未登记、但能展示人物关键表述、产品操作、实验过程或前后差异的媒体，必须把它当作“媒体线索”继续追溯，而不能因主材料页面没有直接播放器就略过。优先采用作者、机构或产品方发布的可播放原件；对标站自托管的字幕、剪辑或转码版本只有在官方发布页已核实但没有可直接嵌入文件时才可采用，并在注册表中如实记录加工页、官方上游页面和媒体血缘。对标站仍不作为事实证据，装饰动画与有效发言视频必须分别作出采用或省略决定。
- 网页因 JavaScript 渲染、反爬或登录状态而改用浏览器提取正文并通过 `--from-text` 继续时，浏览器提取不能只保存 `innerText`。必须限定到目标文章容器，另外导出其中的内容图片、`video/source`、音频、海报、懒加载 URL、最近章节和页面角色，并在渲染前写回 pack 的 `Article.media_assets`。无限滚动页或文章流只审计目标文章容器，不得把相邻文章的媒体混入。
- 发布前执行媒体三方对账：目标文章容器的媒体清单、`Article.media_assets` 注册表、成品的 `source_media` / `listening_cards` 必须能按标准化 URL 或 `media_id` 对应。目标文章内任何视频或音频未进入注册表时直接阻止发布；已登记但未采用的中心演示或首屏叙事视频必须写入 `media_omissions` 并说明它为何只是装饰、重复或与论证无关，不能静默遗漏。最终浏览器验收还要确认实际 `<video>` / `<audio>` 数量、媒体 URL、播放器可见性和窄屏宽度。
- HTML 写盘前必须运行 `media_audit.py`，按 `media_id` 和标准化 URL 对比注册表、采用决定与最终 `<img>`、`<video>`、`<audio>`、`<iframe>` 节点；采用项缺失或 URL 被替换时直接阻止交付。Python 对账证明的是已发现素材的链路完整，动态发现失败仍须先解决或由用户显式选择降级。
- 抓取器为来源媒体保留图注、最近章节、页面顺序、媒体角色、图中来源标签和上游链接候选。`evidence_gallery` 只聚合已登记、确有论证价值的媒体并按 URL 去重；正文采用的来源媒体和数字故事引用的证据图会自动进入页末折叠图库。
- 正文图片和视频的可见图注只解释画面内容，不追加“素材来源、来源素材、出处”等审计链接。媒体 URL、上游页面和来源角色继续保留在 JSON、页首资料来源及页末原始证据图库中，不能因隐藏正文链接而丢失溯源数据。
- 正文来源图片按原始比例直接插入，不使用固定高度横框、黑色补边、播放器式背景或装饰边框；只有视频保留必要的黑色播放器背景和高度限制。移动端同样不能为了统一高度裁切或补黑边。
- 未独立核验时，把结论写成“原文声称”并说明边界；不得把发现但未读取的链接算作已核验。
- 编辑审校必须完整读取研究账本与草稿，并把所有实际修订落实到可确定合并的补丁；多模式或显式完整协议继续返回完整修订稿。未知字段、无效章节、重复章节更新或缺失补丁会自动触发完整修订回退。空段、重复段、缺失高优先级内容、无效证据引用或断裂叙事不得发布。
- 编辑审校必须逐句检查语法、搭配、指代、成分残缺和句式杂糅，并实际返回修订结果，不能用质量报告代替修改。本地语言门禁只自动修复不改变事实的高置信错误；其余疑似语病必须指出字段位置并阻止严格发布。
- `quick_scan` 渲染为标题后的“一分钟速览”：严格 3 条、每条 35-60 字、总计不超过 180 字，依次交代核心变化、实际意义和证据边界。只使用普通纵向列表，不做第二套目录、思维导图或横向压缩卡；它负责先给结论，目录负责章节导航，两者不能重复。
- `sections[].tag` 只用于内部分类、旧数据兼容和视觉元素定位。最终 HTML 与 Markdown 的目录及正文小标题只展示自然标题，不显示 `[风向]`、`[机制]` 等方括号标签；标题里若重复写入同一个 tag，发布时一并清除。
- 完整文章的 HTML 左侧固定目录并随章节高亮；目录展开后，点击目录外的页面区域或按 Escape 自动收起，只保留可再次打开的目录入口。术语解释不再占用右侧栏。每个名词只在正文第一次出现时紧跟一个小型 `i` 脚标：桌面端鼠标移入或键盘聚焦时，在脚标附近浮现术语、定义和通俗理解，移开或失焦即消失；点击或触屏轻点可固定展开，再点空白处或按 Escape 关闭。只有确有必要的原文引文才使用右侧“原文引文”栏，并在正文保留“相关引文 [n]”。Markdown 没有浮层，因此内联保留名词解释和必要引文。
- 完整文章滚动超过首屏后显示“返回文章顶部”箭头。控件必须使用真实锚点链接保证无 JavaScript 时仍可用，并以脚本增强为平滑滚动；提供可访问名称和悬停提示，不能只放一个无功能图标。
- `fact_check` 完整保留在 JSON 和质量门禁中，不在发布页铺陈核查账本。HTML 页末统一使用三段式资料区：“来源”展示主材料标题、作者、发布日期和链接；“延伸”展示 1-5 条实际读取且有助于继续理解的补充材料；“本站说明”读取简短的 `site_note`，用一两句自然语言交代来源属性和关键边界，完整审计仍留在 `source_notes` 与 `fact_check`。
- 客户可见的 HTML、Markdown 产物不得显示“由 article-distiller 生成”、功能清单、输出格式或通用免责声明等生成器痕迹。必要的来源、证据状态和结论边界只在内容需要的位置及“资料与边界”中自然呈现。
- 原始材料的标题链接、作者和发布日期不占用文章首屏。HTML 将其放入文章末尾的统一资料区，与延伸阅读和本站说明分行展示；结构化数据与 Markdown frontmatter 仍完整保留来源字段。
- 首屏主题标签用于长期归档，不承担摘要或观点表达。使用 3-5 个稳定的大类名词，按“主体公司或机构、产品或模型家族、技术领域、应用领域、发布类型”选择；例如 `Google`、`Gemini`、`语音识别`、`语音交互`。每个中文标签控制在 2-8 个汉字，纯英文标签不超过 12 个字符。禁止把完整判断、动作、数字、证据边界或营销话术塞进标签；`AI`、`科技`、`行业` 等无法有效归档的过宽词也不单独使用。
- 成品正文必须像一篇独立文章，不能反复用“原文、原博客、本文、当前材料”等过程话术解释写作来源。证据边界用具体主体和自然语言表达，例如“LTX 尚未公布测试集”或“没有独立复现”；`原文声称` 等审计标签只放在事实核查和来源说明中。
- 主标题先争取点击，再兑现内容。允许从原题事实中提炼冲突、反差、后果、具体人物或读者最在意的问题，使用更口语、更有悬念的营销表达；不必逐字贴着原题，也不能编造主体、动作和结果。未经证实的“第一、唯一、彻底”等强断言优先改成疑问、引语或正文首屏立即解释的编辑钩子。
- 原材料涉及高认知公司、平台、产品或人物时，标题必须把该主体的常用名称逐字放在前半句，再接具体动作、反常结果或用户后果，形成“高认知主体 + 做了什么 + 明确结果”的点击理由。`title_contract.recognition_anchor` 只填写这一个必须进入标题的常用名称，不能写成长句；最终逐字检查它是否出现在标题中。陌生芯片、模型、项目或论文代号不能单独占据标题第一认知位，应写成“OpenAI 的 Jalapeno”“Anthropic 发布……”这类能立即说明归属的结构。不能为了借势把“采集设备特征”升级成“监控个人”等证据不支持的指控。
- 新闻、案件和事件型材料必须在首屏导语及第一节开头优先交代“谁、何时、何地、做了什么、结果怎样”。先让读者看懂发生了什么，再解释证据、机制和边界；不得以方法论、术语、免责声明或核查过程开场。
- 默认按聪明的 AI 初学者校准表达：首次术语先给直观意思，复杂机制拆成动作和因果，读者无需查阅外部资料也能复述文章主线。通俗不等于删除条件，深度不等于堆叠术语。
- 研究和技术文章首屏先给一个真实工程选择、现实代价或反常结果，再解释术语、参数和实验规模；不能让高密度数字抢走读者尚未建立的问题。
- 结尾优先把全文压缩为2至4个有先后顺序、读者可以实际回答的问题，沿阅读方向纵向展示。它已经承担行动判断时，不再追加重复的行动卡或带走清单。
- 成稿后对标属于内部质量流程。默认自动执行并把真正可复用的模式同步到说明、生成提示、渲染器或门禁；不在每次交付中重复向用户说明对标边界。只有发现新增事实、事实冲突、夸大或来源风险时才显式报告。
- 文章必须给出一个可由材料推出的独特判断，并让它在开头被提出、正文被论证、结尾被回答；不得用泛泛的行业感想冒充洞察。
- 全文只允许一个核心机制：用一句“因为……所以……”解释中心问题，其他事实、案例、术语和观点只能证明、解释、限制或应用它。并列争夺中心的观点必须降为支撑或删除。
- 共鸣来自目标读者真实存在的担心、误解、错过或判断困难。可以准确复述一个合理异议及其成立条件，再解释材料增加了什么；不得替读者编造内心戏，也不得先把读者写笨再纠正。
- 动笔前先形成内部“阅读契约”：标题和开头承诺读者得到什么，正文必须走完哪条主链，读完后读者能作出什么判断。与这份契约无关的背景、术语和旁枝主动舍弃，不把内部契约原样展示给读者。
- 深度文章必须执行“真实、讲透、人话、朋友感”四维审校：事实和判断分开，机制解释到因果层，术语首次出现时先给直观理解，语气像懂行朋友认真说明而不是研究报告宣读。不得靠虚构场景、夸张悬念或连续反问制造趣味。
- 标题、小标题和视觉卡片标题必须写清主体与具体动作。遇到“如何被接住、被托住、被兜住、跑通闭环”等不说明动作的抽象表达，优先改成“谁发现、调查、处理、回滚或验证了什么”；隐喻没有增加准确含义时不用。
- 解释型长文的关键章节可以采用“读者问题 → 直接答案 → 证据与原因 → 适用边界”的问答循环。问题必须具体、可回答，正文前 1-2 句先给答案；但不要把每一节都写成同一种问答模板，其他章节可从事件、结果、数据、例子、冲突或明确判断进入。
- 可以在关键章节末尾使用 `transition_hook` 提出读者此刻自然会追问的问题，作为下一节的阅读钩子。它是承上启下的次级提示，不是章节标题：发布页用较小字号、次级文字色和竖线识别，与后续 `h2` 保持明显层级差。全文通常 3-5 处，下一节的问题标题可以压缩重述同一疑问，但不能逐字重复；不得编造悬念或连续堆叠多个问句，最后一节通常留空。
- 每节只完成一个清楚任务，并至少增加新事实、新区别、新机制或新后果。相邻章节避免重复同一种开场动作和内部结构；能删掉而不削弱中心论证的段落应删除。
- 每节先让读者知道“为什么值得继续读”，再展开事实和机制；长内容拆成有节奏的短段。避免连续使用“值得注意的是、综上所述、从某种意义上、不难发现”等报告套话，也避免整篇反复使用“不是……而是……”制造伪深刻。
- 单一事件型文章必须先搭“叙事发动机”：首屏 300-450 个汉字内出现具体处境、反常结果和中心问题，顺序是先让事情发生，再解释背景与术语。来源声明、样本限制、概念定义和抽象判断不得抢占开场。
- 当已核实材料里有能代表中心矛盾的具体小事、人物动作、案例或数字时，优先用它们搭建读者容易进入的背景，再揭示事件主体与核心机制。例子必须来自已读取证据，不能虚构“你一定经历过”的场景；首屏仍须交代谁在何时做了什么，且同一组例子移到开头后不得在后文完整重复。
- 每个核心章节至少完成一种推进：新发现、因果答案、认知反转、现实后果或可执行选择。只有背景罗列、术语解释或重复边界的章节必须合并或删除；单一事件通常优先组织为 4-6 个核心推进动作。
- 证据边界放在对应事实讲清之后，用一两句自然收住。严谨不等于频繁踩刹车，不要让连续免责声明拆散调查、案例或机制的高潮。
- 交付前只读首屏、各节首句和结尾，确认读者能依次回答“发生了什么怪事、抓到了什么、为什么会这样、与我有什么关系、我该怎样判断或行动”。若连续章节都以“作者、材料、数据、这意味着”开场，或删除某节不影响推进链，必须重写或合并。
- 交付前做一次脱离标题、摘要、提纲和来源说明的冷读：正文自身必须说清在回答什么、关键材料怎样支持判断、最后完成了什么结果或选择。再把最后两段分别删掉试读，删后更有力就提前结束。
- 交付前做一次“过度完美检查”：清理段段金句、匀速句长、固定连接词、连续反转、虚构反问、假脆弱、假故事和无必要升华。用自然节奏、准确处境和具体判断增加人味，不照搬任何作者的粗口、口癖、网络梗或故意病句，也不虚构第一人称经历。
- 文章注释分为“类比”“名词解释”“原文引文”三种语义。原文引文默认不生成，全文最多 2 条；只有当原句措辞不可替代、转述会损失关键含义，或争议性主张需要读者核对措辞时才保留。正文已讲清的事实、数字、结论和已有证据图支持的信息不得重复摘引。必要引文必须把原句与中文释义放在同一个编号旁注中；HTML 正文只留“相关引文 [n]”，Markdown 内联输出完整引用。
- 行动卡与带走清单表达相近时只保留一个，避免结尾连续复述同一组建议。

## 交付

优先交付 HTML 和 Markdown，并说明实际抓取了哪些独立材料、哪些结论仍仅来自原文。修改共享脚本后，同步项目测试副本并运行行为测试、Python 编译和 Skill validator。

Skill 根目录以当前 `SKILL.md` 所在目录为准。运行时数据默认写入 `~/.article-distiller/`，可由 `ARTICLE_DISTILLER_DATA_DIR` 覆盖；不得把个人历史索引或绝对路径打入发行包。

发布前运行 `scripts/release_check.py`、全套行为测试、Python 编译和 Skill validator；发布检查必须从独立解包目录验证 `scripts/run.py`，不能只测试已安装副本。
